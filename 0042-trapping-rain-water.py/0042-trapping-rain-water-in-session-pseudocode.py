class Solution:
    def trap(self, height: List[int]) -> int:
        
        # edge cases:
        # if length of input < 3, return 0

        # assume length of 3 or greater

        # max_height = max(height)

        # water_found = 0

        # do the things :)

        # two loops:
    
    
        # moving top to bottom:
        # for row in (max_height downto 0)
        #   sweeping left to right:
        #   bool: in_water = False
        #   bool: in_wall = False
        #   cur_left_wall = 0
        #   cur_right_wall = len(height)-1
        #   for col in range(0, len(height)):
        #       if (not in_water) and (not in_wall):
        #           if height[col] >= row:
        #           in_wall = True
        #           cur_left_wall = col
        #       

    

        # return water_count

        