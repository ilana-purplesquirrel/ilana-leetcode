class Solution:
    def threeSum(self, nums: List[int]) -> List[List[int]]:
    
    # three pointers
    # walk the pointers through the input,
    # generating all possible triplets from the input:
    # three nested loops:
    # i,j,k
    # time complexity O(n^3):
    # for i from 0 to len(num)-3
    #       for j from i+1 to len(num)-2
    #           for k from j+1 to len(num)-1


                    # check the sum of each triplet and save unique triplets that have added to zero

                    # TODO: check for uniqueness
